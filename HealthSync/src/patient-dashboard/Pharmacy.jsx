import styles from "./All-CSSs/TaskButtons.module.css";

const Pharmacy = () => {
   return (
      <button
         className={`${styles.button89} ${styles.posPharmacy}`}
         role='button'
      >
         Pharmacy
      </button>
   );
};

export default Pharmacy;
