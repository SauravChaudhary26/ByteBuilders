import styles from "./All-CSSs/TaskButtons.module.css";

const Payments = () => {
   return (
      <button
         className={`${styles.button89} ${styles.posPayments}`}
         role='button'
      >
         Payments
      </button>
   );
};

export default Payments;
